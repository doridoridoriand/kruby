gemfile: kubernetes/Gemfile
rvm:
  - 3.3
  - 3.4

before_install:
  - gem update --system
  - gem install bundler

script:
- cd kubernetes
- bundle exec rubocop ./src
- bundle exec rubocop ./spec
- bundle exec rspec
